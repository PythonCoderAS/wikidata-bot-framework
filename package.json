{
  "name": "prettier-user-project",
  "version": "1.0.0",
  "private": true,
  "description": "Project only consuming prettier for formatting",
  "scripts": {
    "lint": "prettier --write .",
    "lint:check": "prettier --check .",
    "pyright": "pyright --warnings",
    "mdlint": "markdownlint --fix docs",
    "mdlint:check": "markdownlint --fix --output docs",
    "all": "npm run lint && npm run pyright && npm run mdlint",
    "all:check": "npm run lint:check && npm run pyright && npm run mdlint:check"
  },
  "devDependencies": {
    "markdownlint": "^0.40.0",
    "markdownlint-cli": "^0.48.0",
    "prettier": "^3.8.1",
    "pyright": "^1.1.408"
  }
}
